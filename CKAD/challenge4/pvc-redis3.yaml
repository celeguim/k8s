apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis3
  labels:
    app: redis3
spec:
  storageClassName: hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      app: redis3
