apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis0
  labels:
    app: redis0
spec:
  storageClassName: hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      app: redis0
